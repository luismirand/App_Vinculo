<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Necesario para Firebase y funcionalidades usadas -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Ubicación (en CrearPostActivity) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- Cámara -->
    <uses-permission-sdk-23 android:name="android.permission.CAMERA" /> <!-- Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" /> <!-- Lectura (<= 12) y escritura (<= 9) -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_binku_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_binku_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompraYVenta">
        <activity
            android:name=".ComentariosActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize|stateHidden" />
        <activity
            android:name=".UserProfileActivity"
            android:exported="false" />
        <activity
            android:name=".MiAppFirebase"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name" />
        <activity
            android:name=".ChatRoomActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".CrearPostActivity"
            android:exported="false" />
        <activity
            android:name=".EditarPerfil"
            android:exported="false" />
        <activity
            android:name=".Registro_email"
            android:exported="false" />
        <activity
            android:name=".Opciones_Login.Login_email"
            android:exported="false" /> <!-- Punto de entrada principal -->
        <activity
            android:name=".OpcionesLogin"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>
    

</manifest>